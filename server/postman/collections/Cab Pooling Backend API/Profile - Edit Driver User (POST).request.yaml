$kind: http-request
name: Profile / Edit Driver User (POST)
description: >-
  Updates driver profile fields using the legacy POST variant.


  - Requires a driver bearer token.

  - Supports partial updates to driver profile fields and optional document
  URLs.

  - Do not send `onboarding_status` directly.
url: "{{baseUrl}}/v1/profile/driver/edit-user"
method: POST
headers:
  Authorization: Bearer {{driverAccessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "vehical_type": "Sedan",
      "passenger_capacity": 3
    }
examples: ./.resources/Profile - Edit Driver User (POST).resources/examples
order: 13000
